
@model IEnumerable<Employee>
@{
    //
    ViewBag.Title = "Employee List";
}

@*<table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Department</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in Model)
            {
                <tr>
                    <td> @employee.Id</td>
                    <td> @employee.Name</td>
                    <td> @employee.Department</td>
                </tr>
            }
        </tbody>
    </table>*@

<div class=" card-deck">
    @foreach (var employee in Model)
    {
        <div class=" card m-3">
            <div class=" card-header text-center">
                <h3>@employee.Name</h3>
            </div>
            <img class="card-img-top" src="~/images/elephant.jpg" asp-append-version="true" />
            <div class="card-footer text-center">
                <a asp-controller="Home" asp-action="Details" asp-route-id="@employee.Id" class="btn btn-primary">View</a>
                @*<a href="@Url.Action("details", "home", new {id = employee.Id })" class="btn btn-primary">View</a>*@ 
                @*@Html.ActionLink("View","details", "home", new { id = employee.Id})*@
                @*<a href="#" class="btn btn-primary">View</a>*@
                <a href="#" class="btn btn-primary">Edit</a>
                <a href="#" class="btn btn-danger">Delete</a>
            </div>
        </div>
    }

</div>
