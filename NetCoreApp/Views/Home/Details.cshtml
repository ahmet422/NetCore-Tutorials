
@model HomeDetailsViewModel
@{

    ViewBag.Title = "Employee Details";
    var photPath = "~/images/" + (Model.Employee.PhotoPath ?? "noPic.jgp"); // ?? "noimage.jgp" is for cases if we have no image path
}
@*<h3>
        @Model.PageTitle
    </h3>

    <div>Name: @Model.Employee.Name</div>
    <div>Email:@Model.Employee.Email</div>
    <div>Department:@Model.Employee.Department</div>*@

<div class="row justify-content-center m-3">
    <div class="col-sm-8">
        <div class="card">
            <div class="card-header">
                <h1>@Model.Employee.Name</h1>
            </div>

            <div class=" card-body text-center">
                <img class="card-img-top" src="@photPath" asp-append-version="true" />

                <h4>Employee ID: @Model.Employee.Id</h4>
                <h4>Email : @Model.Employee.Email</h4>
                <h4>Depatrment : @Model.Employee.Department</h4>

            </div>
            <div class="card-footer text-center">
                <a href="@Url.Action("")" class="btn btn-primary">Back</a>
                <a href="#" class="btn btn-primary">Edit</a>
                <a href="#" class="btn btn-danger">Delete</a>
            </div>

        </div>

    </div>

</div>


@section Scripts{
    <script src="~/js/CustomScript.js"></script>
}